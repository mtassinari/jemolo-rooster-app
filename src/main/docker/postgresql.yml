version: '2'
services:
  jemoloroosterapp-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/jemoloRoosterApp/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=jemoloRoosterApp
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
